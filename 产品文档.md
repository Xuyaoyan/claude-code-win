# Claude Terminal Launcher - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品定位
Windows 文件资源管理器扩展，允许用户在任意文件夹中一键打开 Windows Terminal 并执行 Claude 命令。

### 1.2 核心价值
- 提升开发者工作效率，减少手动切换目录和输入命令的时间
- 无缝集成到 Windows 文件管理器，符合用户操作习惯
- 可配置化，支持自定义命令和终端类型

### 1.3 目标用户
- 使用 Claude CLI 工具的开发者
- 需要频繁在特定目录执行命令的 Windows 用户
- 希望提升工作流效率的技术人员

---

## 2. MVP 功能范围

### 2.1 核心功能（必须实现）

#### 功能 1: 工具栏按钮
- **描述**: 在文件资源管理器的命令栏（Ribbon）添加自定义按钮
- **位置**: 主页标签或快速访问工具栏
- **行为**: 点击按钮在当前目录打开 Windows Terminal 并执行 `claude` 命令
- **优先级**: P0

#### 功能 2: 右键菜单集成
- **描述**: 在文件夹空白处右键菜单添加"Open Claude Terminal"选项
- **位置**: 右键菜单 > Open Claude Terminal
- **行为**: 在当前目录打开 Windows Terminal 并执行 `claude` 命令
- **优先级**: P1

#### 功能 3: 配置文件支持
- **描述**: 使用 TOML 配置文件自定义命令和显示名称
- **配置项**:
  - 终端类型（Windows Terminal / PowerShell）
  - 基础命令（默认: `claude`）
  - 菜单显示名称
  - 按钮图标路径
- **优先级**: P0

#### 功能 4: 安装/卸载工具
- **描述**: 提供一键安装和卸载程序
- **功能**:
  - 注册 Shell Extension DLL
  - 写入注册表配置
  - 清理卸载残留
- **优先级**: P0

### 2.2 非 MVP 功能（后续版本）
- [ ] 支持自定义命令参数选择
- [ ] 多命令快捷启动
- [ ] 图形化配置界面
- [ ] 命令执行历史记录
- [ ] 支持其他终端（CMD, Git Bash 等）

---

## 3. 技术方案

### 3.1 技术栈选择

#### 主项目: C++ Shell Extension
- **语言**: C++17
- **框架**: ATL (Active Template Library)
- **编译器**: MSVC 2022
- **目标平台**: Windows 10/11 x64

#### 配置管理: C++ TOML Parser
- **库**: toml++ (https://github.com/marzer/tomlplusplus)
- **格式**: TOML 1.0

#### 安装程序: C++ CLI
- **功能**: 注册表操作、DLL 注册、权限提升

### 3.2 架构设计

```
ClaudeTerminalLauncher/
├── ClaudeShellExt/          # Shell Extension DLL
│   ├── ClaudeCommand.cpp    # IExplorerCommand 实现
│   ├── DllMain.cpp          # DLL 入口
│   ├── Registry.cpp         # COM 注册
│   └── Config.cpp           # TOML 配置读取
├── Installer/               # 安装/卸载程序
│   └── main.cpp
├── resources/               # 资源文件
│   ├── icon.ico
│   └── config.toml.template
└── docs/                    # 文档
    └── 产品文档.md
```

### 3.3 核心接口实现

#### IExplorerCommand 接口
```cpp
class ClaudeCommand :
    public IExplorerCommand,
    public IObjectWithSite
{
public:
    // 获取按钮显示文本
    IFACEMETHODIMP GetTitle(IShellItemArray* items, LPWSTR* name);

    // 获取按钮图标
    IFACEMETHODIMP GetIcon(IShellItemArray* items, LPWSTR* icon);

    // 按钮点击事件
    IFACEMETHODIMP Invoke(IShellItemArray* items, IBindCtx* bc);

    // 按钮状态（启用/禁用）
    IFACEMETHODIMP GetState(IShellItemArray* items, BOOL okToBeSlow, EXPCMDSTATE* state);
};
```

### 3.4 注册表配置

#### Shell Extension 注册
```
HKEY_LOCAL_MACHINE\SOFTWARE\Classes\CLSID\{GUID}
├── (Default) = "Claude Terminal Launcher"
├── InprocServer32
│   └── (Default) = "path\to\ClaudeShellExt.dll"
└── ThreadingModel = "Apartment"

HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\CommandStore\shell\ClaudeLauncher
├── (Default) = "Open Claude Terminal"
├── Icon = "path\to\icon.ico"
└── command
    └── (Default) = "wt.exe -d \"%V\" powershell -NoExit -Command \"claude\""
```

---

## 4. 配置文件规范

### 4.1 config.toml 示例

```toml
[terminal]
# 终端类型: "wt" (Windows Terminal) 或 "powershell"
type = "wt"
# Windows Terminal 路径（留空则使用系统默认）
path = ""

[command]
# 基础命令（固定部分）
base = "claude"
# 默认参数（可选）
default_args = []
# 是否保持终端打开（-NoExit）
keep_open = true

[ui]
# 菜单显示名称
menu_text = "Open Claude Terminal Here"
# 按钮工具提示
tooltip = "Launch Windows Terminal with Claude CLI"
# 图标路径（留空使用默认图标）
icon_path = ""

[registry]
# COM GUID（安装时自动生成，请勿修改）
clsid = ""
# 注册表键名
menu_key = "ClaudeTerminalLauncher"
```

---

## 5. 用户流程

### 5.1 安装流程
1. 下载安装包（installer.exe）
2. 以管理员身份运行安装程序
3. 安装程序执行：
   - 复制 DLL 到 `%ProgramFiles%\ClaudeTerminalLauncher\`
   - 注册 COM 组件（regsvr32）
   - 写入注册表配置
   - 创建默认 config.toml
4. 重启 Windows Explorer（explorer.exe）
5. 安装完成

### 5.2 使用流程

#### 场景 1: 使用工具栏按钮
1. 打开文件资源管理器
2. 导航到目标文件夹
3. 点击工具栏"Open Claude Terminal"按钮
4. Windows Terminal 在当前目录启动并执行 `claude`

#### 场景 2: 使用右键菜单
1. 打开文件资源管理器
2. 导航到目标文件夹
3. 在空白处右键 → 选择"Open Claude Terminal Here"
4. Windows Terminal 在当前目录启动并执行 `claude`

### 5.3 卸载流程
1. 以管理员身份运行 `installer.exe uninstall`
2. 安装程序执行：
   - 注销 COM 组件（regsvr32 /u）
   - 删除注册表项
   - 删除程序文件
3. 重启 Windows Explorer
4. 卸载完成

---

## 6. 开发计划

### 6.1 里程碑

#### Phase 1: 环境搭建（1天）
- [x] 确定技术方案
- [x] 编写 PRD 文档
- [ ] 安装 Visual Studio 2022 + Windows SDK
- [ ] 创建 C++ 项目结构

#### Phase 2: 核心功能开发（3-4天）
- [ ] 实现 IExplorerCommand 接口
- [ ] 实现 COM DLL 注册
- [ ] 实现配置文件读取
- [ ] 实现 Windows Terminal 启动逻辑

#### Phase 3: 右键菜单集成（1天）
- [ ] 添加注册表配置
- [ ] 测试右键菜单功能

#### Phase 4: 安装程序开发（1-2天）
- [ ] 实现安装程序
- [ ] 实现卸载程序
- [ ] 添加管理员权限提升

#### Phase 5: 测试与优化（1-2天）
- [ ] 功能测试
- [ ] 兼容性测试（Windows 10/11）
- [ ] 性能优化
- [ ] Bug 修复

#### Phase 6: 文档与发布（1天）
- [ ] 编写用户文档
- [ ] 编写开发者文档
- [ ] 准备发布包
- [ ] GitHub Release

**总计开发时间: 7-10天**

---

## 7. 依赖与环境

### 7.1 开发环境
- **操作系统**: Windows 10 21H2+ 或 Windows 11
- **IDE**: Visual Studio 2022 Community
- **SDK**: Windows 11 SDK (10.0.22621.0+)
- **编译器**: MSVC v143 (Visual Studio 2022)

### 7.2 运行环境
- **操作系统**: Windows 10 1903+ 或 Windows 11
- **运行时**: Visual C++ Redistributable 2022
- **终端**: Windows Terminal (推荐) 或 PowerShell 5.1+

### 7.3 第三方库
- **toml++**: Header-only TOML 解析库
- **ATL**: COM 开发框架（Visual Studio 自带）

---

## 8. 风险评估

### 8.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| Windows 安全策略阻止 DLL 注入 | 高 | 中 | 使用代码签名证书 |
| COM 注册需要管理员权限 | 中 | 高 | 安装程序自动提升权限 |
| Windows 11 对 Shell Extension 限制 | 高 | 低 | 测试多个 Windows 版本 |
| Explorer 崩溃导致系统不稳定 | 高 | 低 | 严格测试，添加异常处理 |

### 8.2 用户体验风险
- **学习成本**: 新用户可能不知道按钮位置 → 提供详细文档和截图
- **兼容性问题**: 旧版 Windows 不支持 → 明确最低系统要求

---

## 9. 成功指标

### 9.1 功能完整性
- [ ] 工具栏按钮可正常显示和点击
- [ ] 右键菜单可正常显示和点击
- [ ] Windows Terminal 正确启动并切换到目标目录
- [ ] `claude` 命令正确执行
- [ ] 配置文件可正常读取和解析

### 9.2 稳定性
- [ ] 无内存泄漏
- [ ] 无 Explorer 崩溃
- [ ] 支持高频点击（10次/秒不卡顿）

### 9.3 兼容性
- [ ] Windows 10 21H2 测试通过
- [ ] Windows 11 23H2 测试通过
- [ ] 与其他 Shell Extension 无冲突

---

## 10. 后续迭代方向

### v1.1 (MVP + 1)
- 添加命令参数选择界面
- 支持多个预设命令

### v1.2
- 图形化配置工具
- 支持更多终端类型

### v2.0
- 命令历史记录
- 快捷键支持
- 多语言界面

---

## 附录

### A. 参考资料
- [IExplorerCommand 接口文档](https://docs.microsoft.com/en-us/windows/win32/api/shobjidl_core/nn-shobjidl_core-iexplorercommand)
- [Shell Extension 开发指南](https://docs.microsoft.com/en-us/windows/win32/shell/shell-exts)
- [ATL COM 编程](https://docs.microsoft.com/en-us/cpp/atl/)

### B. 变更记录
| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| 1.0 | 2025-09-28 | 初始版本 | Claude |

---

**文档状态**: 已完成
**最后更新**: 2025-09-28
**审核状态**: 待审核